/*function soma() {

    return 5 + 2
}

console.log(soma())

function subtracao() {

    return 5 - 2
}

console.log(subtracao())


function soma(valor1, valor2, valor3) {

    return valor1 + valor2 + valor3
}

console.log(soma(3, 3, 5))


function div(valor1, valor2) {

    return valor1 / valor2
}

console.log(div(10, 3))


function fruta(fruta) {
    let text = '';
    if (fruta == 'abacaxi') {
        text = 'Abacaxi é um fruto azedo'
    } else if (fruta == 'melancia') {
        text = 'Melancia é um fruto doce'
    } return text
}

console.log(fruta('pera'));

function adivinharNum (num) {
    if (num === 10) {
        text = `Seu número foi ${num}`
    }
    else if (num === 15) {
        text = `Seu número foi ${num}`
    }
    else if (num === 20) {
        text = `Seu número foi ${num}`
    }
    else {
        text = `Seu número não foi sorteado`
    }
    return text
}
console.log(adivinharNum(10))

function getSizes(alt, lar, com) {
    let area = alt * lar;
    let volume = alt * lar * com;
    let sizes = [area, volume];
    return sizes;
}

console.log(getSizes(3,3,2));
console.log(getSizes(3, 2, 2,[0]));
console.log(getSizes(3, 3, 2[1]));

function somar(num) {
    for (let m = 1; m <= 10; m++) {
        console.log(`${m} + ${num} = ${m + num}`);
    }
}

function multiplicar(num) {
    for (let l = 1; l <= 10; l++) {
        console.log(`${l} * ${num} = ${l * num}`);
    }
}
somar(5)
console.log("-----------------");
multiplicar(5)

function pularNumero(num) {
    for (let i = 1; i<=5; i++) {
        if (i === num){
            continue;
        }
        console.log(`O número é: ${i}`);
    }
}
pularNumero(2);

function somarNumeros(numerozinho) {
    let soma = 0;
    numerozinho.forEach(i=> {
        soma += i;
    });
    return soma;
}

console.log(somarNumeros([1, 2, 3, 4]));*/